name: "Node Js ci"
# Define the events that trigger the workflow
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
# start jobs
jobs
  build: 
    runs-on: "ubuntu-latest"
    # Step 1: Checkout the repository