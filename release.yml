name: "Release With Changelog"
on:
  push:
    branches: ["main"]
jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      #Checkout code
      - name: Checkout code
        uses: actions/checkout@v4
